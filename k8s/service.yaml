apiVersion: v1
kind: Service
metadata:
  name: notifications-api
spec:
  selector:
    app: notifications-api
  type: NodePort
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8082
      nodePort: 30002